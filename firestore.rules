rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections for now (no auth)
    // TODO: Add Firebase Authentication and restrict by user
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
